<?jelly escape-by-default='true'?>

<j:jelly xmlns:j="jelly:core" xmlns:f="/lib/form">
  <f:entry field="label" title="${%Label}" description="The label used to identify this agent(node) in Jenkins.">
    <f:textbox />
  </f:entry>

  <f:entry field="functionName" title="${%Function Name}" description="AWS Lambda Function name or ARN.">
    <f:select />
  </f:entry>

</j:jelly>
